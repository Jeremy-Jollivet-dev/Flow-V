<form method="post" action="{{ path('app_parcours_delete', {'id': parcour.id}) }}" onsubmit="return confirm('Voulez vous supprimer ce parcours ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ parcour.id) }}">
    <button class="button">Suprimmer</button>
</form>
