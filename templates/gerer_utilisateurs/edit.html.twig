{% extends 'base.html.twig' %}

{% block title %}Modifier Utilisateurs{% endblock %}

{% block body %}
    <h1>Modifier Utilisateurs</h1>
    {{ form_start(form) }}
    <div class="container-form">
        <div class="form-group">
            {{ form_label(form.pseudo) }} 
            <br>
            {{ form_widget(form.pseudo) }}
            
        </div>
        <div class="form-group ">
            {{ form_label(form.role) }}
            <br>
            {{ form_widget(form.role) }}
            
        </div>
    </div>
    {{ include('gerer_utilisateurs/_form.html.twig', {'button_label': 'Mettre a Jour'}) }}
    {{ form_end(form) }}
    <br>
    <a class="button" href="{{ path('app_gerer_utilisateurs_index') }}">Retour à la liste</a>
    <br>
    <br>
    {{ include('gerer_utilisateurs/_delete_form.html.twig') }}
{% endblock %}
